pywebview>=5.0
playwright>=1.40
pyinstaller>=6.0
# CA trust store for urllib/ssl. A frozen Windows build has no system CA
# bundle on ssl's default path, so HTTPS calls (update check, installer
# download, distribution PDF, Claude API) fail with URLError without this.
certifi>=2024.2.2
